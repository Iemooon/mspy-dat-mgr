# Public test fixtures

This repository deliberately does not contain exported dictionaries, imported Microsoft Pinyin DAT files, or cached workspaces from a real user.

The automated tests build minimal synthetic DAT samples in temporary directories. They are only for parser and round-trip regression checks; they are not Microsoft Pinyin compatibility samples.
